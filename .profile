# path
export PATH=$HOME/bin/:$HOME/.ghcup/bin/:$HOME/.cabal/bin/:$HOME/.cargo/bin/:$HOME/jdk/bin/:$PATH

# TODO fix colors
# prompt
# export PS1='$(whoami)@$(hostname):$(pwd)$ '

# colors (FreeBSD, Mac OS)
export CLICOLOR=1

export EDITOR=vim
export VISUAL=vim
